<dx:ThemedWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:local="clr-namespace:TechEquipments"
    xmlns:views="clr-namespace:TechEquipments.Views.Param"
    xmlns:infoviews="clr-namespace:TechEquipments.Views.Info"
    xmlns:msgviews="clr-namespace:TechEquipments.Views.Message"
    x:Class="TechEquipments.MainWindow"
    Icon="/TechEquipments;component/Resources/Icons/app.ico"
    Title="{Binding Vm.Shell.WindowTitle}"
    Height="800" Width="1200" PreviewKeyDown="ThemedWindow_PreviewKeyDown" WindowState="Maximized">

    <dx:ThemedWindow.Resources>
        <!-- Конвертер bool -> Visibility -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <DataTemplate x:Key="StationItemTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Style="{StaticResource AppTextBlockStyle}"/>

                <Image Width="16" Height="16" Margin="10,0,6,0" VerticalAlignment="Center"
                       Visibility="{Binding IsOffline, Converter={StaticResource BoolToVis}}"
                       ToolTip="No connection to this station"
                       Source="{dx:DXImage 'DevAV/Priorities/LowPriority_16x16.png'}"/>

            </StackPanel>
        </DataTemplate>


        <!-- Подсветка событий/типов -->
        <local:EventKeyToBrushConverter x:Key="EventKeyToBrush"/>
        <local:EquipTypeGroupToBrushConverter x:Key="TypeGroupToBrush"/>

        <!-- SVG иконка Scan QR из ресурсов проекта -->
        <dx:SvgImageSource x:Key="ScanQrGlyph" Uri="/TechEquipments;component/Resources/Icons/scan_qr.svg"/>
    </dx:ThemedWindow.Resources>

    <Grid>
        <!-- 2 зоны: основное содержимое + нижняя панель прогресса -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================= MAIN AREA ================= -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <!-- LEFT -->
                <ColumnDefinition x:Name="LeftCol" Width="350" MinWidth="0"/>
                <!-- SEPARATOR -->
                <ColumnDefinition x:Name="SepCol" Width="1"/>
                <!-- RIGHT -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ============ LEFT PANEL ============ -->
            <Border Grid.Column="0" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Верх: Station + Type в одной строке -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Station -->
                        <dxe:ComboBoxEdit Grid.Row="0" Grid.Column="0" PopupMinHeight="160"
                                          HorizontalContentAlignment="Left"
                                          NullText="Stations"
                                          IsTextEditable="False"
                                          ItemsSource="{Binding Vm.EquipmentList.Stations}"
                                          DisplayMember="Name"
                                          ValueMember="Name"
                                          ItemTemplate="{StaticResource StationItemTemplate}"
                                          ApplyItemTemplateToSelectedItem="True"
                                          EditValue="{Binding Vm.EquipmentList.SelectedStation, UpdateSourceTrigger=PropertyChanged}">
                            
                        </dxe:ComboBoxEdit>

                        <!--<TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center"
                                   Text="Station"
                                   Style="{StaticResource AppLabelTextStyle}"/>-->

                        <!-- Type -->
                        <dxe:ComboBoxEdit Grid.Row="0" Grid.Column="2"
                                          HorizontalContentAlignment="Left"
                                          NullText="Types"
                                          IsTextEditable="False"
                                          ItemsSource="{Binding TypeFilters}"
                                          ItemTemplate="{StaticResource EquipTypeFilterItemTemplate}"
                                          ApplyItemTemplateToSelectedItem="True"
                                          EditValue="{Binding Vm.EquipmentList.SelectedTypeFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            
                        </dxe:ComboBoxEdit>

                        <!--<TextBlock Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center"
                                   Text="Type"
                                   Style="{StaticResource AppLabelTextStyle}"/>-->
                    </Grid>

                    <!-- Список оборудования -->
                    <dxg:TreeListControl x:Name="EquipmentsTree"
                                         Grid.Row="1"
                                         ItemsSource="{Binding EquipmentsView}"
                                         AutoGenerateColumns="None"
                                         SelectedItem="{Binding Vm.EquipmentList.SelectedListBoxEquipment, Mode=TwoWay}"
                                         SelectedItemChanged="EquipmentsTree_SelectedItemChanged"
                                         BorderThickness="0">

                        <dxg:TreeListControl.View>
                            <dxg:TreeListView x:Name="EquipmentsTreeView"
                                              TreeDerivationMode="Selfreference"
                                              KeyFieldName="NodeId"
                                              ParentFieldName="ParentNodeId"
                                              RootValue="0"
                                              AutoExpandAllNodes="True"
                                              AutoWidth="True"
                                              RowMinHeight="36"
                                              ShowColumnHeaders="False"
                                              ShowIndicator="False"
                                              ShowHorizontalLines="False"
                                              ShowVerticalLines="False"
                                              AllowPerPixelScrolling="True"
                                              AllowSorting="False"
                                              AllowColumnMoving="False"
                                              AllowColumnFiltering="False"
                                              AllowEditing="False"
                                              NavigationStyle="Row"/>
                        </dxg:TreeListControl.View>

                        <dxg:TreeListControl.Columns>
                            <dxg:TreeListColumn FieldName="DisplayText"
                                                Width="*"
                                                MinWidth="120"
                                                AllowEditing="False"
                                                AllowSorting="False"
                                                AllowFocus="False">
                                <dxg:TreeListColumn.CellTemplate>
                                    <StaticResource ResourceKey="EquipmentTreeCellTemplate"/>
                                </dxg:TreeListColumn.CellTemplate>
                            </dxg:TreeListColumn>
                        </dxg:TreeListControl.Columns>
                    </dxg:TreeListControl>

                    <!-- Версия -->
                    <TextBlock Grid.Row="2"                               
                               Margin="0,6,-10,-5"
                               VerticalAlignment="Bottom"
                               HorizontalAlignment="Left"                               
                               Text="{Binding AppVersionText}"/>
                </Grid>

            </Border>

            <!-- ============ RIGHT PANEL ============ -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- TOP TOOLBAR -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,8,8,8">

                    <!-- Кнопка скрытия/показа левой панели -->
                    <ToggleButton x:Name="LeftPaneToggle"
                                  Width="45"
                                  Margin="0,0,8,0"
                                  Style="{StaticResource AppToggleStyle}"
                                  Click="LeftPaneToggle_Click">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border Padding="0"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="#C8C8C8"
                                        BorderThickness="1">
                                    <Grid Width="16" Height="16">
                                        <Path x:Name="ArrowLeft"
                                              Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                              StrokeThickness="2"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              Data="M 11,3 L 5,8 L 11,13"
                                              Visibility="Collapsed"/>

                                        <Path x:Name="ArrowRight"
                                              Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                                              StrokeThickness="2"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              Data="M 5,3 L 11,8 L 5,13"
                                              Visibility="Visible"/>
                                    </Grid>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#10FFFFFF"/>
                                    </Trigger>

                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#1A000000"/>
                                    </Trigger>

                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="ArrowLeft" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="ArrowRight" Property="Visibility" Value="Collapsed"/>
                                    </Trigger>

                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter TargetName="ArrowLeft" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="ArrowRight" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>

                    <!-- ===== DB Date (видимо только на DB вкладках) ===== -->
                    <TextBlock Text="Date:"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"
                               Style="{StaticResource AppTextBlockStyle}"
                               Visibility="{Binding Vm.IsDbTabSelected, Converter={StaticResource BoolToVis}}"/>

                    <dxe:DateEdit Width="140"
                                  Margin="0,0,12,0"
                                  Style="{StaticResource AppBaseEditStyle}"
                                  EditValue="{Binding DbDate, UpdateSourceTrigger=PropertyChanged}"
                                  Visibility="{Binding Vm.IsDbTabSelected, Converter={StaticResource BoolToVis}}"/>

                    <!-- ===== Search (всегда) ===== -->
                    <dxe:TextEdit x:Name="SearchTextEdit"
                                  Width="240"
                                  Style="{StaticResource AppBaseEditStyle}"
                                  HorizontalContentAlignment="Left"
                                  NullText="Equipment name..."
                                  EditValue="{Binding Vm.EquipmentList.EquipName, UpdateSourceTrigger=PropertyChanged}"/>

                    <!-- ===== ONE multifunction button ===== -->
                    <dx:SimpleButton Content="{Binding Vm.MainActionButtonText}"
                                     Width="110"
                                     Margin="8,0,0,0"
                                     Style="{StaticResource AppButtonStyle}"
                                     IsEnabled="{Binding Vm.CanMainAction}"
                                     Click="MainAction_Click"
                                     Visibility="{Binding Vm.ShowMainActionButton, Converter={StaticResource BoolToVis}}"/>

                </StackPanel>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,8,0" HorizontalAlignment="Right">
                    
                    <!-- ===== Scan QR (показываем только на Param) ===== -->
                    <dx:SimpleButton Width="40" 
                                     Height="40"
                                     Margin="8,0,0,0"
                                     Padding="0"
                                     GlyphWidth="32"
                                     GlyphHeight="32"
                                     Click="ToolbarScanQr_Click"
                                     ToolTip="Scan QR"
                                     Glyph="{StaticResource ScanQrGlyph}"
                                     Style="{StaticResource AppButtonStyle}"/>
                    <!--Visibility="{Binding ShowToolbarScanQrButton, Converter={StaticResource BoolToVis}}"/>-->

                    <Image Height="40" Width="152" Margin="10,0,0,0" Stretch="Uniform" SnapsToDevicePixels="True" Source="/TIS_logo_intellegence_g.png" Cursor="Hand"/> <!--MouseLeftButtonUp="Logo_MouseLeftButtonUp"-->
                </StackPanel>

                <!-- TABS -->
                <dx:DXTabControl x:Name="MainTabs"
                                 Grid.Row="1"
                                 Style="{StaticResource AppTabControlStyle}"
                                 ItemContainerStyle="{StaticResource AppTabItemStyle}"
                                 SelectedIndex="{Binding SelectedMainTabIndex, Mode=TwoWay}">

                    <!-- ============ PARAM TAB ============ -->
                    <dx:DXTabItem>

                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Margin="0,0,6,0" Source="{dx:DXImage 'SvgImages/Spreadsheet/LightBlueDataBarSolid.svg'}" />
                                <TextBlock Text="Param" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                            <!-- Вынесенный UI вкладки Param -->
                        <views:ParamTabHost DataContext="{Binding Vm.Param}" />
                    </dx:DXTabItem>

                    <!-- ============ INFO TAB ============ -->
                    <dx:DXTabItem IsEnabled="{Binding Vm.Info.IsInfoDbConnected}">

                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Margin="0,0,6,0" Source="{dx:DXImage 'SvgImages/Icon Builder/Actions_Info.svg'}" />
                                <TextBlock Text="Info" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                        <infoviews:InfoTabHost DataContext="{Binding Vm.Info}"/>
                    </dx:DXTabItem>

                    <!-- ============ OPERATION ACTIONS TAB ============ -->
                    <dx:DXTabItem IsEnabled="{Binding Vm.Database.IsDbConnected}">

                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Margin="0,0,6,0" Source="{dx:DXImage 'SvgImages/Business Objects/BO_MyDetails.svg'}" />
                                <TextBlock Text="Operation actions" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                        <Grid>

                            <!-- Таблица -->
                            <dxg:GridControl ItemsSource="{Binding Vm.Database.OperatorActRows}" AutoGenerateColumns="None">
                                <dxg:GridControl.View>
                                    <dxg:TableView Style="{StaticResource AppTableViewStyle}"
                                                   AutoWidth="False"
                                                   ShowGroupPanel="False"
                                                   AllowPerPixelScrolling="True"
                                                   ColumnHeaderStyle="{StaticResource AppGridColumnHeaderStyle}"/>
                                </dxg:GridControl.View>

                                <dxg:GridControl.Columns>
                                    <!--<dxg:GridColumn FieldName="Date" Header="Date" ReadOnly="True" Width="90"/>-->
                                    <dxg:GridColumn FieldName="Time" Header="Time" ReadOnly="True" Width="80"/>
                                    <!--<dxg:GridColumn FieldName="Type" Header="Type" ReadOnly="True" Width="60"/>-->
                                    <!--<dxg:GridColumn FieldName="Client" Header="Client" ReadOnly="True" Width="140"/>-->
                                    <dxg:GridColumn FieldName="User" Header="User" ReadOnly="True" Width="100"/>
                                    <!--<dxg:GridColumn FieldName="Tag" Header="Tag" ReadOnly="True" Width="220"/>-->
                                    <dxg:GridColumn FieldName="Equip" Header="Equip" ReadOnly="True" Width="200"/>
                                    <dxg:GridColumn FieldName="Desc" Header="Desc" ReadOnly="True" Width="*"/>
                                    <dxg:GridColumn FieldName="OldV" Header="Old" ReadOnly="True" Width="100"/>
                                    <dxg:GridColumn FieldName="NewV" Header="New" ReadOnly="True" Width="100"/>
                                </dxg:GridControl.Columns>
                            </dxg:GridControl>
                        </Grid>
                    </dx:DXTabItem>

                    <!-- ============ ALARM HISTORY TAB ============ -->
                    <dx:DXTabItem IsEnabled="{Binding Vm.Database.IsDbConnected}">

                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Margin="0,0,6,0" Source="{dx:DXImage 'SvgImages/Business Objects/BO_Audit_ChangeHistory.svg'}" />
                                <TextBlock Text="Alarm history" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                        <Grid>

                            <dxg:GridControl ItemsSource="{Binding Vm.Database.AlarmHistoryRows}" AutoGenerateColumns="None">
                                <dxg:GridControl.View>
                                    <dxg:TableView Style="{StaticResource AppTableViewStyle}"
                                                   AutoWidth="False"
                                                   ShowGroupPanel="False"
                                                   AllowPerPixelScrolling="True"
                                                   ColumnHeaderStyle="{StaticResource AppGridColumnHeaderStyle}"/>
                                </dxg:GridControl.View>

                                <dxg:GridControl.Columns>
                                    <!--<dxg:GridColumn FieldName="Date" Header="Date" ReadOnly="True" Width="90"/>-->
                                    <dxg:GridColumn FieldName="Time" Header="Time" ReadOnly="True" Width="80"/>
                                    <dxg:GridColumn FieldName="Category" Header="Category" ReadOnly="True" Width="120"/>
                                    <dxg:GridColumn FieldName="User" Header="User" ReadOnly="True" Width="140"/>
                                    <!--<dxg:GridColumn FieldName="Location" Header="Location" ReadOnly="True" Width="120"/>-->
                                    <dxg:GridColumn FieldName="Equipment" Header="Equipment" ReadOnly="True" Width="200"/>
                                    <dxg:GridColumn FieldName="Item" Header="Item" ReadOnly="True" Width="110"/>
                                    <dxg:GridColumn FieldName="Comment" Header="Comment" ReadOnly="True" Width="*"/>
                                    <dxg:GridColumn FieldName="State" Header="State" ReadOnly="True" Width="150"/>
                                </dxg:GridControl.Columns>
                            </dxg:GridControl>
                        </Grid>
                    </dx:DXTabItem>

                    <!-- ============ SOE TAB ============ -->
                    <dx:DXTabItem Visibility="{Binding Vm.IsSoeTabVisible, Converter={StaticResource BoolToVis}}">

                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="20" Height="20" Margin="0,0,6,0" Source="{dx:DXImage 'SvgImages/Spreadsheet/Hyperlink.svg'}" />
                                <TextBlock Text="SOE" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                        <dxg:GridControl ItemsSource="{Binding equipmentSOEDtos}" AutoGenerateColumns="None">
                            <dxg:GridControl.Columns>
                                <dxg:GridColumn FieldName="TimeLocal" Header="Date" Width="90" MinWidth="90" MaxWidth="90" ReadOnly="True">
                                    <dxg:GridColumn.EditSettings>
                                        <dxe:DateEditSettings Mask="dd.MM.yyyy" MaskUseAsDisplayFormat="True"/>
                                    </dxg:GridColumn.EditSettings>
                                </dxg:GridColumn>

                                <dxg:GridColumn FieldName="TimeLocal" Header="Time" Width="90" MinWidth="90" MaxWidth="90" ReadOnly="True">
                                    <dxg:GridColumn.EditSettings>
                                        <dxe:DateEditSettings Mask="HH:mm:ss" MaskUseAsDisplayFormat="True"/>
                                    </dxg:GridColumn.EditSettings>
                                </dxg:GridColumn>

                                <dxg:GridColumn FieldName="TypeGroup" Header="Type" Width="110" MinWidth="110" MaxWidth="110" ReadOnly="True"/>
                                <dxg:GridColumn FieldName="Equipment" Header="Equipment" Width="160" MinWidth="160" MaxWidth="360" ReadOnly="True"/>

                                <!-- Подсветка Event по ключу -->
                                <dxg:GridColumn FieldName="Event" Header="Event" Width="220" ReadOnly="True">
                                    <dxg:GridColumn.CellStyle>
                                        <Style TargetType="dxg:LightweightCellEditor">
                                            <Setter Property="Background" Value="{Binding RowData.Row.EventKey, Converter={StaticResource EventKeyToBrush}}"/>
                                            <Setter Property="Padding" Value="4,0"/>
                                        </Style>
                                    </dxg:GridColumn.CellStyle>
                                </dxg:GridColumn>
                            </dxg:GridControl.Columns>

                            <dxg:GridControl.View>
                                <dxg:TableView Style="{StaticResource AppTableViewStyle}"
                                               AutoWidth="True"
                                               ShowGroupPanel="False"
                                               AllowPerPixelScrolling="True"
                                               ColumnHeaderStyle="{StaticResource AppGridColumnHeaderStyle}"/>
                            </dxg:GridControl.View>
                        </dxg:GridControl>
                    </dx:DXTabItem>

                    <!-- ============ MESSAGE TAB ============ -->
                    <dx:DXTabItem>
                        <dx:DXTabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{dx:DXImage 'SvgImages/Icon Builder/Actions_Comment.svg'}"
                                       Width="16"
                                       Height="16"
                                       Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Vm.Message.TabHeader}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </dx:DXTabItem.Header>

                        <msgviews:MessageTabHost DataContext="{Binding Vm.Message}"/>
                    </dx:DXTabItem>

                </dx:DXTabControl>
            </Grid>
        </Grid>

        <!-- ================= BOTTOM PROGRESS BAR ================= -->
        <!-- Общая нижняя панель: используется для загрузки EquipList и DB -->
        <Border Grid.Row="1"
                Padding="10,6"
                Background="#0A000000"
                Visibility="{Binding Vm.IsBottomStatusVisible, Converter={StaticResource BoolToVis}}">

            <DockPanel LastChildFill="True">
                <!-- Текст статуса -->
                <TextBlock DockPanel.Dock="Left"
                           Style="{StaticResource AppTextStyle}"
                           Text="{Binding Vm.BottomStatusText}"
                           Foreground="{Binding Vm.BottomStatusBrush}"
                           Margin="0,0,12,0"/>
                <!--<TextBlock DockPanel.Dock="Left"
                           Style="{StaticResource AppTextStyle}"
                           Text="{Binding Vm.BottomText}"
                           Margin="0,0,12,0"/>-->

                <!-- Один ProgressBar:- EquipList: Value/Maximum- DB: IsIndeterminate=true -->
                <ProgressBar Height="16"
                             Minimum="0"
                             Maximum="{Binding Vm.BottomProgressMaximum, Mode=OneWay}"
                             Value="{Binding Vm.BottomProgressValue, Mode=OneWay}"
                             IsIndeterminate="{Binding Vm.BottomProgressIsIndeterminate, Mode=OneWay}"
                             Visibility="{Binding Vm.IsBottomLoading, Converter={StaticResource BoolToVis}}"/>

            </DockPanel>
        </Border>

        <!-- ================= LOADING OVERLAY (SOE) ================= -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              Panel.ZIndex="999"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Visibility="{Binding Vm.Shell.IsLoading, Converter={StaticResource BoolToVis}}">

            <Border Background="#66000000"/>

            <Border Background="White"
                    CornerRadius="10"
                    Padding="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                <Grid Width="420">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок overlay: Trend оставляем как было -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Text="{Binding Vm.Shell.LoadingText}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,12,0"/>

                        <dx:SimpleButton Grid.Column="1"
                                         Content="Cancel"
                                         Width="90"
                                         Style="{StaticResource AppButtonStyle}"
                                         Click="Cancel_Click"/>
                    </Grid>

                    <!-- Progress 1 (Current) -->
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <dxe:ProgressBarEdit Height="18"
                                             Minimum="0"
                                             Maximum="{Binding Vm.Shell.PerTrendMax}"
                                             EditValue="{Binding Vm.Shell.CurrentCount, Mode=OneWay}"/>

                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   IsHitTestVisible="False">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="current: {0}/{1}">
                                    <Binding Path="Vm.Shell.CurrentCount"/>
                                    <Binding Path="Vm.Shell.PerTrendMax"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Grid>

                    <!-- Progress 2 (Rows) -->
                    <Grid Grid.Row="2" Margin="0,8,0,0">
                        <dxe:ProgressBarEdit Height="18"
                                             Minimum="0"
                                             Maximum="{Binding Vm.Shell.TotalMax}"
                                             EditValue="{Binding Vm.Shell.LoadedCount, Mode=OneWay}"/>

                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   IsHitTestVisible="False">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="rows: {0}/{1}">
                                    <Binding Path="Vm.Shell.LoadedCount"/>
                                    <Binding Path="Vm.Shell.TotalMax"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</dx:ThemedWindow>
