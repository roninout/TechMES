@page "/info"
@inject NavigationManager NavigationManager

<PageTitle>Info</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/equipment", replace: true);
    }
}
